<div>
<h3 align="center"><%= "Title: #{@title}"%></h3>
<div class="container" style="Width: 400px; position: relative;  right: 350px; ">
<form class="form-group" method="POST" action="/submit/<%= @title %>">
    <%=
        story_file = File.open(Dir.pwd+ "/Radlib/" + "#{@title}" << ".txt","r"){  |file|
            line =  file.readline 
            # line.to_s.gsub(/(\w+[^\[\w+\]])/) { |original_story_words| original_story_words = "<b class=\"text-primary\">#{original_story_words}</b>"}
            
            new_additions = line.to_s.gsub(/(\[\w+\])/) {|part_of_speech| 
               pos_name = part_of_speech 
               part_of_speech =   
               " 
                <label class=\"col-form-label-sm\">Enter #{pos_name}:</label>
                <input class=\"form-control form-control-sm\ type=\"text\"></input>
               "
               
            }
                # "<p class=\"text-primary\"style=\"font-weight: bold;\">" +  new_additions + "</p>"
        }

    %> 
        <input type="submit">
     </form> 
    </div>
<br/>

</div>


